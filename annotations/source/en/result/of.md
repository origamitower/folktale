@annotate: folktale.result.of
category: Constructing
---

Constructs a Result holding an Ok value.


## Example::

    const Result = require('folktale/result');
    
    Result.of(1);
    // ==> Result.Ok(1)
    
